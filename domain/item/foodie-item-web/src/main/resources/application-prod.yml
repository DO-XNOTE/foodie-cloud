server:
  port: 80

spring:
  datasource:                                         # 数据源的相关配置 localhost如果是分布式要写云服务器内网的ip地址
    url: jdbc:mysql://localhost:3306/foodie-shop-dev?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true
    password: immoc